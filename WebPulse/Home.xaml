<UserControl x:Class="WebPulse.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebPulse"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
     <Grid.RowDefinitions>
       <RowDefinition Height="Auto"/>
       <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Border Grid.Row="0" BorderThickness="1" CornerRadius="10" Width="200" Height="100" Background="#9394A5" Margin="10,50,10,10" Opacity="0.5">
                <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Column="1">
            <Border Grid.Row="0" BorderThickness="1" CornerRadius="10" Width="200" Height="100" Background="#9394A5" Margin="10,50,10,10" Opacity="0.5">
                <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Column="2">
            <Border Grid.Row="0" BorderThickness="1" CornerRadius="10" Width="200" Height="100" Background="#9394A5" Margin="10,50,10,10" Opacity="0.5">
                <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
    <Grid Grid.Row="1">
        <Border BorderThickness="1" CornerRadius="10" Width="auto" Height="auto" Background="#9394A5" Margin="50,50,50,5" Opacity="0.5">
            <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            </StackPanel>
        </Border>
    </Grid>
            
    </Grid>
</UserControl> 
<!-- Left, Top, Right, Bottom -->