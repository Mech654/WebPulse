<UserControl x:Class="WebPulse.Monitor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebPulse"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,40,40,0">
            <Button Width="50" Height="50" Background="#9394A5" BorderBrush="DarkBlue" BorderThickness="2">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="2" Width="50" Height="50" CornerRadius="25">
                            <TextBlock Text="+" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>



        </Grid>

        <Grid Grid.Row="1" Background="Green">
            <TextBlock Text="This is the second row" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="20"/>
        </Grid>
    </Grid>
</UserControl>
